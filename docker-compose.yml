version: '3'

services:
  payload:
    build: .
     # Uncomment when not set via local ENV.
    # environment:
      # - PAYLOAD_SECRET=FOR_TESTING_ONLY_FROM_COMPOSE
      
    ports:
      - 3000:3000
    volumes:
      - db:/app/db
      - uploads:/app/uploads

volumes:
  db:
  uploads: